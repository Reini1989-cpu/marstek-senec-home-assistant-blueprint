# GitHub Upload

Kopiere den Inhalt dieses ZIPs in dein Repository.

Wichtig: Der Ordner `custom_components` und die Datei `hacs.json` müssen direkt im Hauptverzeichnis deines GitHub-Repositories liegen.

Danach:

1. committen
2. Release `v1.0.0` erstellen
3. in HACS als Custom Repository mit Kategorie `Integration` testen
